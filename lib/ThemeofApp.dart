import 'package:flutter/material.dart';

final appTheme = ThemeData(
  primarySwatch: Colors.deepPurple,
  textTheme: TextTheme(
    display1: TextStyle(
    fontFamily: 'Georgia',
    fontSize: 20,
    color: Colors.black,
  ),
),
);